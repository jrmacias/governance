# <System> - Constitution

This document states the principles that are specific to the <System>
system (`<repo-path>`). The platform principles in
`policies/constitution.md` apply in full to this system; this document
adds system-specific principles and does not weaken them. When a
specification or an implementation appears to conflict with either
document, the conflict must be raised with the project owner before
proceeding.

This constitution changes only through explicit approval by the project
owner, for the same reason as specifications.

<!-- Add system-specific principles only, and keep it short. Never repeat
     a platform principle here. Good candidates: the public API as a
     contract, dependency policy, architecture invariants,
     environment-specific constraints. -->

## 1. <System-specific principle>

One or two sentences stating the invariant and why it exists.
